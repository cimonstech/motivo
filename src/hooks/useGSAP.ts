"use client";
// Re-export GSAP + ScrollTrigger for consistent usage across components
export { gsap } from "gsap";
export { ScrollTrigger } from "gsap/ScrollTrigger";
export { useGSAP } from "@gsap/react";
